if HAVE_LIBCL
OCL_DIR = ocl
else
OCL_DIR =
endif

if ENABLE_IA_AIQ
ISP_DIR = isp
else
ISP_DIR =
endif

if HAVE_GLES
GLES_DIR = gles
else
GLES_DIR =
endif


SUBDIRS = soft $(ISP_DIR) $(OCL_DIR) $(GLES_DIR)
